import { css } from '@emotion/react';

export default css`
  :root {
    font-smooth: always;
    -webkit-font-smoothing: subpixel-antialiased;
    -moz-osx-font-smoothing: auto;
    font-size: 16px;
  }
`;
